#include <bits/stdc++.h>
using namespace std;
int N;
string s;
int main() {
    cin.sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> N;
    while (N--) {
        cin >> s;
        cout << s.size() << "\n";
    }
    return 0;
}